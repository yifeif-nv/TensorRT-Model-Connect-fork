{
  "models": [
    {
      "model": "ggml-org/stories15M_MOE",
      "family": "mixtral",
      "task": "text_generation",
      "label": "A4-decode-moe",
      "benchmark": {
        "label": "CPU argmax",
        "gpu_argmax_label": "GPU argmax",
        "metric": "tok/s",
        "command": ["{binary}", "run", "{bundle}", "--runtime-root", "{runtime_root}", "--prompt", "{prompt}", "--max-new-tokens", "{max_tokens}"]
      }
    }
  ]
}
